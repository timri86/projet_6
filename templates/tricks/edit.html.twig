{% extends 'base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <h1>Modifier</h1>

    {{ include('tricks/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tricks_index') }}">Retourner à la liste</a>

    {{ include('tricks/_delete_form.html.twig') }}
{% endblock %}
